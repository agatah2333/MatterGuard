name: Water Heater Mode - OnMode Attribute Test
config:
  nodeId: '0x12344321'
  endpoint: 1
  cluster: Water Heater Mode
  timeout: 180
tests:
- label: Read OnMode attribute
  command: readAttribute
  attribute: OnMode
  response:
    saveAs: onmode_value
