name: Water Heater Mode - CurrentMode Attribute Test
config:
  nodeId: '0x12344321'
  endpoint: 1
  cluster: Water Heater Mode
  timeout: 180
tests:
- label: Read CurrentMode attribute
  command: readAttribute
  attribute: CurrentMode
  response:
    saveAs: currentmode_value
