name: Valve Configuration and Control - CurrentState Attribute Test
config:
  nodeId: '0x12344321'
  endpoint: 1
  cluster: Valve Configuration and Control
  timeout: 180
tests:
- label: Read CurrentState attribute
  command: readAttribute
  attribute: CurrentState
  response:
    saveAs: currentstate_value
