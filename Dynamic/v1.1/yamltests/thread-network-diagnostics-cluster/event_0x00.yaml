config:
  cluster: thread-network-diagnostics-cluster
  event: '0x00'
  nodeId: '0x654231'
name: thread-network-diagnostics-cluster_event_0x00_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: '`ConnectionStatus` attribute will be set to `Connected`.'
  ExpectedResponse: Node's connection status to a Thread network has changed to "Connected".
  Pre-requisites: Node must be initially in a "NotConnected" state.
  arguments:
    ConnectionStatus: 0
  option: SubscribeToEvent
- ActionTime: N/A
  Attribute_Change: '`ConnectionStatus` attribute will be set to `NotConnected`.'
  ExpectedResponse: Node's connection status to a Thread network has changed to "NotConnected".
  Pre-requisites: 'Node must be initially in a "Connected" state.


    These test cases cover the extreme values of the `ConnectionStatus` field and
    the special responses as per the event description.


    ### References

    - The details of the `ConnectionStatus` event and its fields are derived from
    the Matter 1.0 Core Specification document【4:2†source】【4:3†source】.'
  arguments:
    ConnectionStatus: 1
  option: SubscribeToEvent
