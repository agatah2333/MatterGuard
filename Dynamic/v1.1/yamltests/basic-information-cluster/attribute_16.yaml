config:
  attribute: '16'
  cluster: basic-information-cluster
  nodeId: '0x654231'
name: basic-information-cluster_attribute_16_v1.1.0.0
tests:
- ActionTime: <current_time>
  Attribute_Change: '{"Reachable": True}'
  ExpectedResponse: <success>
  Pre-requisites: <None>
  arguments:
    '{"Reachable"': True}
  option: WriteAttribute
- ActionTime: <current_time>
  Attribute_Change: '{"Reachable": False}'
  ExpectedResponse: <failure>
  Pre-requisites: '<None>


    These test cases cover the extreme values of the `Reachable` attribute (True and
    False) and the special responses that could result from these values, such as
    successful communication when `Reachable` is True and potential communication
    failure when `Reachable` is False【4:10†source】【4:12†source】.'
  arguments:
    '{"Reachable"': False}
  option: WriteAttribute
