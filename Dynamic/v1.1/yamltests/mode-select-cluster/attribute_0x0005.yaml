config:
  attribute: '0x0005'
  cluster: mode-select-cluster
  nodeId: '0x654231'
name: mode-select-cluster_attribute_0x0005_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: '`CurrentMode` will change to `OnMode` when On/Off state changes
    from OFF to ON.'
  ExpectedResponse: No change in `CurrentMode` when On/Off state changes from ON to
    OFF or OFF to OFF.
  Pre-requisites: '`SupportedModes` attribute must include a mode with value 0.'
  arguments:
    OnMode: 0
  label: Test_Case_Minimum_Value
  option: WriteAttribute
- ActionTime: N/A
  Attribute_Change: '`CurrentMode` will change to `OnMode` when On/Off state changes
    from OFF to ON.'
  ExpectedResponse: No change in `CurrentMode` when On/Off state changes from ON to
    OFF or OFF to OFF.
  Pre-requisites: '`SupportedModes` attribute must include a mode with value 255.'
  arguments:
    OnMode: 255
  label: Test_Case_Maximum_Value
  option: WriteAttribute
- ActionTime: N/A
  Attribute_Change: No attribute will be changed.
  ExpectedResponse: '`OnMode` has no effect on `CurrentMode` regardless of On/Off
    state changes.'
  Pre-requisites: '`OnMode` attribute is set to null.'
  arguments: {}
  label: Test_Case_Special_Response_-_Null_Value
  option: WriteAttribute
- ActionTime: N/A
  Attribute_Change: '`CurrentMode` will be set to the value of `OnMode`.'
  ExpectedResponse: '`CurrentMode` changes to `OnMode` when On/Off state changes from
    OFF to ON.'
  Pre-requisites: '`SupportedModes` attribute must include a mode with value <valid_mode_value>.


    These test cases cover the extreme values of the `OnMode` attribute and the special
    responses as described in the specification【4:0†source】【4:1†source】【4:5†source】【4:9†source】.'
  arguments:
    OnMode: <valid_mode_value>
  label: Test_Case_Special_Response_-_Valid_Mode_Change
  option: WriteAttribute
