config:
  attribute: '0x0000'
  cluster: thermostat-cluster
  nodeId: '0x654231'
name: thermostat-cluster_attribute_0x0000_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: '`TemperatureDisplayMode` will be set to `0` (Celsius)'
  ExpectedResponse: Success
  Pre-requisites: 'Thermostat must support the `TemperatureDisplayMode` attribute.


    **Test Case 1.2**:'
  arguments:
    '{"TemperatureDisplayMode"': 0}
  label: Test_Case_Extreme_Values_of_the_Constraints
  option: WriteAttribute
- ActionTime: N/A
  Attribute_Change: N/A
  ExpectedResponse: '`CONSTRAINT_ERROR`'
  Pre-requisites: 'Thermostat must support the `TemperatureDisplayMode` attribute
    and should return an error for invalid values.


    These test cases cover the extreme values of the constraints and the special responses
    for the `TemperatureDisplayMode` attribute in the Thermostat Cluster.


    For more detailed information, you can refer to the Matter Application Cluster
    Specification document【4:0†source】.'
  arguments:
    '{"TemperatureDisplayMode"': 2}
  label: Test_Case_Special_Responses
  option: WriteAttribute
