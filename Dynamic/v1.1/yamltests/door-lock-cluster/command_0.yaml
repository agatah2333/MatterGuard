config:
  cluster: door-lock-cluster
  command: '0'
  nodeId: '0x654231'
name: door-lock-cluster_command_0_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: LockState to Locked
  ExpectedResponse: SUCCESS or FAILURE depending on `RequirePINforRemoteOperation`
  Pre-requisites: '`RequirePINforRemoteOperation` is false'
  arguments:
    PINCode: '""'
  label: Test_Case_Minimum_Value_for_PINCode
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: LockState to Locked
  ExpectedResponse: SUCCESS
  Pre-requisites: '`RequirePINforRemoteOperation` is true'
  arguments:
    PINCode: '"<maximum_length_string>"'
  label: Test_Case_Maximum_Length_for_PINCode
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: None (LockState remains unchanged)
  ExpectedResponse: FAILURE (counts towards WrongCodeEntryLimit)
  Pre-requisites: '`RequirePINforRemoteOperation` is true'
  arguments:
    PINCode: '"<invalid_pin>"'
  label: Test_Case_Invalid_PINCode
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: LockState to Locked
  ExpectedResponse: SUCCESS
  Pre-requisites: '`RequirePINforRemoteOperation` is true


    ### Notes:

    - The `PINCode` field is optional but required if the `RequirePINforRemoteOperation`
    attribute is true.

    - An invalid PIN will count towards the `WrongCodeEntryLimit` and trigger `UserCodeTemporaryDisableTime`
    if the limit is exceeded.

    - The lock will ignore attempts to lock/unlock the door until the `UserCodeTemporaryDisableTime`
    expires.


    These test cases cover the extreme values of the constraints of the field and
    the values that could result in special responses as mentioned in the description
    of the effect【4:0†source】【4:3†source】.'
  arguments:
    PINCode: '"<valid_pin>"'
  label: Test_Case_Valid_PINCode
  option: SendCommand
