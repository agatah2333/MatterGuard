config:
  cluster: door-lock-cluster
  command: '37'
  nodeId: '0x654231'
name: door-lock-cluster_command_37_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: User entry values reset to default values (UserStatus = Available,
    UserType = UnrestrictedUser), all associated credentials and schedules cleared.
  ExpectedResponse: SUCCESS
  Pre-requisites: At least one user exists.
  arguments:
    '{"UserIndex"': 1}
  label: Test_Case_Minimum_Value_of_UserIndex
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: User entry values reset to default values (UserStatus = Available,
    UserType = UnrestrictedUser), all associated credentials and schedules cleared.
  ExpectedResponse: SUCCESS
  Pre-requisites: The maximum number of users is supported and exists.
  arguments:
    '{"UserIndex"': NumberOfTotalUsersSupported}
  label: Test_Case_Maximum_Value_of_UserIndex
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: All user entries reset to default values (UserStatus = Available,
    UserType = UnrestrictedUser), all associated credentials and schedules cleared.
  ExpectedResponse: SUCCESS
  Pre-requisites: Multiple users exist.
  arguments:
    '{"UserIndex"': 0xFFFE}
  label: Test_Case_Special_Value_to_Clear_All_Users
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: None
  ExpectedResponse: CONSTRAINT_ERROR
  Pre-requisites: 'N/A


    These test cases cover the extreme values of the constraints of the field and
    the special values that could result in specific responses as mentioned in the
    command description【4:3†source】.'
  arguments:
    '{"UserIndex"': 0}
  label: Test_Case_Invalid_UserIndex_Value
  option: SendCommand
