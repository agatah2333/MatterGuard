config:
  cluster: media-playback-cluster
  command: '0x06'
  nodeId: '0x654231'
name: media-playback-cluster_command_0x06_v1.1.0.0
tests:
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` will be set to -1'
  ExpectedResponse: SUCCESS
  Pre-requisites: Media must be playing in the forward direction or not playing.
  arguments:
    '{"PlaybackSpeed"': -1}
  label: Test_Case_Minimum_Rewind_Speed
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` will be set to -N'
  ExpectedResponse: SUCCESS
  Pre-requisites: Media must be playing in the forward direction or not playing.
  arguments:
    '{"PlaybackSpeed"': -N}
  label: Test_Case_Maximum_Rewind_Speed
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: No change
  ExpectedResponse: SPEED_OUT_OF_RANGE
  Pre-requisites: Media must be playing in the forward direction or not playing.
  arguments:
    '{"PlaybackSpeed"': -N-1}
  label: Test_Case_Speed_Out_of_Range
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: No change
  ExpectedResponse: NOT_ALLOWED
  Pre-requisites: 'Media must be live streaming content not supporting seek.


    These test cases cover the extreme values of the constraints of the `PlaybackSpeed`
    field and the special responses mentioned in the description of the effect of
    the `Rewind` command【4:0†source】【4:2†source】.'
  arguments:
    '{"PlaybackSpeed"': -1}
  label: Test_Case_Not_Allowed
  option: SendCommand
