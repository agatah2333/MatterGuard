config:
  cluster: media-playback-cluster
  command: '0x02'
  nodeId: '0x654231'
name: media-playback-cluster_command_0x02_v1.1.0.0
tests:
- ActionTime: N/A
  Attribute_Change: '- `CurrentState` will be set to `NOT_PLAYING`'
  ExpectedResponse: SUCCESS
  Pre-requisites: Media must be in a playing state.
  arguments: {}
  label: Test_Case_Extreme_Value_of_Constraints
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: '- `CurrentState` will be set to `NOT_PLAYING`'
  ExpectedResponse: SUCCESS
  Pre-requisites: 'Media must be in a playing state. The user-visible outcome may
    navigate the user back to the location from where the media was originally launched.


    These test cases cover the extreme values and special responses as per the command''s
    description in the Media Playback Cluster specification【4:0†source】【4:1†source】.'
  arguments: {}
  label: Test_Case_Special_Response_-_Context-Specific_User-Visible_Outcome
  option: SendCommand
