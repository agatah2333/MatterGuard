config:
  cluster: dishwasher-mode-cluster
  command: '0x01'
  nodeId: '0x654231'
name: dishwasher-mode-cluster_command_0x01_v1.2.0.0
tests:
- ActionTime: N/A
  Attribute_Change: '`CurrentMode` will be set to the value of `NewMode`.'
  ExpectedResponse: Success
  Pre-requisites: The `NewMode` field in the `ChangeToMode` command must match the
    value in the `SupportedModes` attribute list.
  arguments:
    Status: '0x00'
    StatusText: '""'
  label: Test_Case_Extreme_Value_(Minimum)
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: No attribute will be changed.
  ExpectedResponse: Manufacturer-specific error
  Pre-requisites: The `NewMode` field in the `ChangeToMode` command must not match
    any value in the `SupportedModes` attribute list.
  arguments:
    Status: '0xBF'
    StatusText: '"Maximum error code reached"'
  label: Test_Case_Extreme_Value_(Maximum)
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: No attribute will be changed.
  ExpectedResponse: UnsupportedMode
  Pre-requisites: The `NewMode` field in the `ChangeToMode` command does not match
    any entries in the `SupportedModes` attribute list.
  arguments:
    Status: '0x01'
    StatusText: '""'
  label: Test_Case_Special_Response_(Unsupported_Mode)
  option: SendCommand
- ActionTime: N/A
  Attribute_Change: No attribute will be changed.
  ExpectedResponse: InvalidInMode
  Pre-requisites: 'The current mode of the device does not allow the transition to
    the `NewMode` specified in the `ChangeToMode` command.


    These test cases cover the extreme values of the constraints of the fields and
    the values that could result in special responses as mentioned in the description
    of the effect【4:0†source】【4:1†source】【4:2†source】【4:12†source】.'
  arguments:
    Status: '0x03'
    StatusText: '""'
  label: Test_Case_Special_Response_(Invalid_In_Mode)
  option: SendCommand
