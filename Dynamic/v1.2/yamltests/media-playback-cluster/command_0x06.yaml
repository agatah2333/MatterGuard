config:
  cluster: media-playback-cluster
  command: '0x06'
  nodeId: '0x654231'
name: media-playback-cluster_command_0x06_v1.2.0.0
tests:
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` remains -2.0'
  ExpectedResponse: SPEED_OUT_OF_RANGE
  Pre-requisites: Media is currently playing in the forward direction.
  arguments:
    CurrentState: 0 (Playing)
    PlaybackSpeed: '-2.0'
  label: Test_Case_Minimum_Playback_Speed
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` remains 2.0'
  ExpectedResponse: NOT_ALLOWED
  Pre-requisites: Media is currently playing in the forward direction.
  arguments:
    CurrentState: 0 (Playing)
    PlaybackSpeed: '2.0'
  label: Test_Case_Maximum_Playback_Speed
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` remains 0'
  ExpectedResponse: INVALID_STATE_FOR_COMMAND
  Pre-requisites: Media is not currently playing.
  arguments:
    CurrentState: 2 (NotPlaying)
    PlaybackSpeed: 0
  label: Test_Case_Rewind_When_Not_Playing
  option: SendCommand
- ActionTime: <current_time>
  Attribute_Change: '`PlaybackSpeed` changes to -1.0'
  ExpectedResponse: SUCCESS
  Pre-requisites: 'Media is currently paused.


    These test cases cover the extreme values of the constraints of the `PlaybackSpeed`
    field and the values of the `CurrentState` field that could result in special
    responses as mentioned in the description of the effect【4:1†source】【4:0†source】.'
  arguments:
    CurrentState: 1 (Paused)
    PlaybackSpeed: 0
  label: Test_Case_Rewind_When_Paused
  option: SendCommand
