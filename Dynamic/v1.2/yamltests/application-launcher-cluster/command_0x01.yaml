config:
  cluster: application-launcher-cluster
  command: '0x01'
  nodeId: '0x654231'
name: application-launcher-cluster_command_0x01_v1.2.0.0
tests:
- ActionTime: Immediate
  Attribute_Change: '- `Status` attribute of the Application Basic cluster of the
    Endpoint corresponding to the stopped application will be updated to `Stopped`.'
  ExpectedResponse: 0 (Success)
  Pre-requisites: The application with the given `CatalogVendorID` and `ApplicationID`
    must be running.
  arguments:
    ApplicationID: '""'
    CatalogVendorID: '0x0000'
  label: Test_Case_Minimum_Values
  option: SendCommand
- ActionTime: Immediate
  Attribute_Change: '- `Status` attribute of the Application Basic cluster of the
    Endpoint corresponding to the stopped application will be updated to `Stopped`.'
  ExpectedResponse: 0 (Success)
  Pre-requisites: The application with the given `CatalogVendorID` and `ApplicationID`
    must be running.
  arguments:
    ApplicationID: '"A" * 256 (assuming 256 is a reasonable maximum length for an
      application identifier)'
    CatalogVendorID: '0xFFFF'
  label: Test_Case_Maximum_Values
  option: SendCommand
- ActionTime: Immediate
  Attribute_Change: '- No attribute changes as the application is not available.'
  ExpectedResponse: 1 (AppNotAvailable)
  Pre-requisites: The application with the given `CatalogVendorID` and `ApplicationID`
    must not be available.
  arguments:
    ApplicationID: '"NonExistentApp"'
    CatalogVendorID: '0x1234'
  label: Test_Case_Application_Not_Available
  option: SendCommand
- ActionTime: Immediate
  Attribute_Change: '- No attribute changes as the system is busy.'
  ExpectedResponse: 2 (SystemBusy)
  Pre-requisites: 'The system must be busy and unable to process the stop command.


    ### References

    - The details for the `StopApp` command and its expected behavior are derived
    from the Matter Application Cluster Specification【4:0†source】【4:1†source】【4:2†source】【4:3†source】【4:4†source】【4:5†source】【4:6†source】【4:7†source】【4:8†source】【4:9†source】【4:10†source】【4:11†source】【4:12†source】【4:13†source】【4:14†source】【4:15†source】【4:16†source】【4:17†source】【4:18†source】【4:19†source】.'
  arguments:
    ApplicationID: '"BusyApp"'
    CatalogVendorID: '0x5678'
  label: Test_Case_System_Busy
  option: SendCommand
